hanzi:
  se:
    front: |
      #### Hanzi Chinese-English

      ```pug parsed
      .font-chinese.text-w-normal(style="font-size: 3rem;") {{item}}
      ```
    back: |
      ```pug parsed
      .inline-block.mr-4 {{raw.pinyin}}
      x-speak-button.speak-item--1 {{item}}
      ```

      {{raw.english}}
  ec:
    front: |
      #### Hanzi English-Chinese

      {{mask raw.english item}}
    back: |
      ```pug parsed
      .hanzi-display {{item}}
      x-speak-button.speak-item--1 {{item}}
      ```

      {{raw.pinyin}}
vocab:
  se:
    front: |
      #### Vocab Simplified-English

      ```pug parsed
      .font-zh-simp.text-w-normal(style="font-size: 2rem;") {{item}}
      ```
    back: |
      ```pug parsed
      {{#if raw.traditional}}
      div
        .font-zh-trad.text-w-normal.inline-block.mr-4(style="font-size: 1.7rem;") {{join raw.traditional ' | '}}
        x-speak-button.speak-item--1 {{item}}
      {{/if}}

      div
        .inline-block.mr-4 {{join raw.pinyin ' | '}}
        {{#unless raw.traditional}}x-speak-button.speak-item--1 {{item}}{{/unless}}

      {{#if raw.english}}
      ul
        {{#each raw.english}}
        li {{this}}
        {{/each}}
      {{/if}}

      {{#if raw.sentences.length}}
      ul
        {{#each raw.sentences}}
        li
          span.font-zh-simp {{chinese}} 
          x-speak-button.speak-item-{{@index}} {{chinese}}
          ul
            li {{english}}
        {{/each}}
      {{/if}}
      ```
  te:
    front: |
      #### Vocab Traditional-English

      ```pug parsed
      .font-zh-trad.text-w-normal(style="font-size: 1.7rem;") {{join raw.traditional ' | '}}
      ```
    back: |
      ```pug parsed
      div
        .font-zh-simp.text-w-normal.inline-block.mr-4(style="font-size: 2rem;") {{item}}
        x-speak-button.speak-item--1 {{item}}

      div {{join raw.pinyin ' | '}}

      {{#if raw.english}}
      ul
        {{#each raw.english}}
        li {{this}}
        {{/each}}
      {{/if}}

      {{#if raw.sentences.length}}
      ul
        {{#each raw.sentences}}
        li
          span.font-zh-simp {{chinese}} 
          x-speak-button.speak-item-{{@index}} {{chinese}}
          ul
            li {{english}}
        {{/each}}
      {{/if}}
      ```
  ec:
    front: |
      #### Vocab English-Chinese

      {{#each raw.english}}
      - {{mask this ../item ../raw.pinyin ../raw.traditional}}
      {{/each}}  
    back: |
      ```pug parsed
      div
        .font-zh-simp.text-w-normal.inline-block.mr-4(style="font-size: 2rem;") {{item}}
        x-speak-button.speak-item--1 {{item}}

      {{#if raw.traditional}}
      .font-zh-trad.text-w-normal(style="font-size: 1.7rem;") {{join raw.traditional ' | '}}
      {{/if}}

      {{#if raw.pinyin}}
      div {{join raw.pinyin ' | '}}
      {{/if}}

      {{#if raw.english}}
      ul
        {{#each raw.english}}
        li {{this}}
        {{/each}} 
      {{/if}}

      {{#if raw.sentences.length}}
      ul
        {{#each raw.sentences}}
        li
          span.font-zh-simp {{chinese}} 
          x-speak-button.speak-item-{{@index}} {{chinese}}
          ul
            li {{english}}
        {{/each}}
      {{/if}}
      ```
sentence:
  se:
    front: |
      #### Sentence Chinese-English

      ```pug parsed
      h2.font-zh-simp.text-w-normal {{item}}
      ```
    back: |
      ```pug parsed
      h2.font-zh-simp.text-w-normal.inline-block {{item}}
      x-speak-button.speak-item--1 {{item}}

      ul
        {{#each raw}}
        li {{pinyin}}
        {{/each}}

      ul
        {{#each raw}}
        li {{english}}
        {{/each}}
      ```
  ec:
    front: |
      #### Sentence English-Chinese

      {{#each raw}}
      - {{english}}
      {{/each}}
    back: |
      ```pug parsed
      div
        span.font-zh-simp {{item}}
        x-speak-button.speak-item--1 {{item}}
      ```

      {{#each raw}}
      - {{pinyin}}
      {{/each}}
extra:
  se:
    front: |
      #### Extra Chinese-English

      ```pug parsed
      h2.font-zh-simp.text-w-normal {{item}}
      ```
    back: |
      ```pug parsed
      div
        .inline-block.mr-4 {{raw.pinyin}}
        x-speak-button.speak-item--1 {{item}}
      ```

      {{raw.english}}
  ec:
    front: |
      #### Extra English-Chinese

      {{raw.english}}
    back: |
      ```pug parsed
      div
        h2.font-zh-simp.text-w-normal.inline-block.mr-4 {{item}}
        x-speak-button.speak-item--1 {{item}}
      ```

      {{raw.pinyin}}
