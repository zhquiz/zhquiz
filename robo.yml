clone:
  command: meta git init
status:
  command: meta git status
dev:
  command: |
    cd submodules/web
    yarn dev
dist:
  command: |
    cd submodules/web
    yarn build
    cd ../server
    robo build-all
    cd ../../packages/nodejs
    yarn ts-node scripts/dist.ts
